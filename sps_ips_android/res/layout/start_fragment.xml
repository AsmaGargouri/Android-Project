<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/relativeLayoutStart"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <Button
        android:id="@+id/chooseMapButton"
        style="@style/button_text"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:background="@drawable/red_button"
        android:onClick="chooseMap"
        android:text="@string/chooseMapButton" />
    
<!-- 
    <Button
        android:id="@+id/Itenerary"
        style="@style/button_text"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_alignLeft="@+id/chooseMapButton"
        android:layout_alignRight="@+id/chooseMapButton"
        android:layout_below="@+id/chooseMapButton"
        android:layout_marginTop="28dp"
        android:background="@drawable/red_button"
        android:onClick="itenerary"
        android:text="@string/itenerary" />
 -->

    <Button
        android:id="@+id/read"
        style="@style/button_text"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_alignRight="@+id/save"
        android:layout_below="@+id/save"
        android:background="@drawable/red_button"
        android:onClick="readImg"
        android:text="read" />

    <Button
        android:id="@+id/save"
        style="@style/button_text"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_below="@+id/chooseMapButton"
        android:layout_centerHorizontal="true"
        android:background="@drawable/red_button"
        android:onClick="saveImg"
        android:text="save" />
     
</RelativeLayout>